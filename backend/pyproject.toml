[project]
name = "definam-backend"
version = "0.1.0"
description = "DefinAm learning platform — FastAPI backend"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "python-dotenv>=1.0.0",
    "pydantic[email]>=2.0",
    "pydantic-settings>=2.0",
    "passlib[bcrypt]>=1.7",
    "resend>=2.0",
    "aiosmtplib>=3.0",
    "sqlalchemy[asyncio]>=2.0",
    "asyncpg>=0.29",
    "httpx>=0.27",
    "alembic>=1.13",
    "jinja2>=3.1",
    "slowapi>=0.1.9",
    "bcrypt<4.0",
    "groq>=0.9.0",
    "google-generativeai>=0.6.0",
    "redis>=5.0.0",
    "pgvector>=0.3.0",
    "google-genai>=2.9.0",
    "pyjwt>=2.13.0",
    "anthropic>=0.115.1",
    "pdfplumber>=0.11.0",
]

[project.optional-dependencies]
dev = [
    "ruff>=0.4.0",
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "httpx>=0.27.0",
]

[tool.uv]
package = false

[tool.ruff]
target-version = "py312"
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
ignore = ["E501", "B008"]

[tool.ruff.lint.per-file-ignores]
"alembic/*" = ["E402", "SIM105"]
"alembic/versions/*" = ["E402", "I001"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]

[dependency-groups]
dev = [
    "locust>=2.45.0",
    "pytest>=9.0.3",
    "pytest-asyncio>=1.3.0",
    "ruff>=0.15.13",
]